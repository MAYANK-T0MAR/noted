import { Metadata } from "next"

export const metadata: Metadata = {
    title : 'Notifications / Noted'
}

export default function notificationsPage() {
    return (
        <div>
            This is notifications page.
        </div>
    )
}
import { Metadata } from "next"


export const metadata: Metadata ={
    title : 'Saved / Noted'
}

export default function savedPage() {
    return (
        <div>
            This is Saved page.
        </div>
    )
}